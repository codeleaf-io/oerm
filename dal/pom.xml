<project>

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.codeleaf.dal</groupId>
        <artifactId>dal</artifactId>
        <version>0.1.0</version>
    </parent>

    <artifactId>dal-dal</artifactId>
    <name>DAL :: Data Access Layer</name>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>dal-object</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>dal-entity</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>dal-record</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

</project>
